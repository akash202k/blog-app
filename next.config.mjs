/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pexels.com", "lh3.googleusercontent.com", "example.com", "res.cloudinary.com"]
    }
};

export default nextConfig;
